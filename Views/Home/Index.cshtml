@model IEnumerable<PcSaler.Models.CategoryProductViewModel>
@{
    ViewData["Title"] = "Trang chủ";
    var categories = ViewBag.Categories as List<PcSaler.DBcontext.Entites.Category>;
    int? selectedCat = ViewBag.SelectedCat as int?;
    string query = ViewBag.Query as string ?? "";
}

<div class="row mb-4">
    <!-- Bên trái: Danh mục -->
    <div class="col-md-3">
        <div class="card p-3" style="height: 450px; overflow-y: auto;">
            <h6>Danh mục</h6>
            <ul class="list-unstyled">
                <li>
                    <a class="text-decoration-none @(selectedCat == null ? "fw-bold" : "")"
                       href="@Url.Action("Index", "Home")">
                        Tất cả
                    </a>
                </li>
                @foreach (var c in categories ?? new List<PcSaler.DBcontext.Entites.Category>())
                {
                    <li class="mb-1">
                        <a class="text-decoration-none @(selectedCat == c.CategoryID ? "fw-bold" : "")"
                           href="@Url.Action("Index", "Home", new { cat = c.CategoryID, q = query })">
                            @c.CategoryName
                        </a>
                    </li>
                }
            </ul>
        </div>
    </div>

    <!-- Bên phải: Banner -->
    <div class="col-md-9">
        <div id="mainCarousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img src="~/images/banner1.jpg" class="d-block w-100 rounded" alt="banner1">
                </div>
                <div class="carousel-item">
                    <img src="~/images/banner2.jpg" class="d-block w-100 rounded" alt="banner2">
                </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#mainCarousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon"></span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#mainCarousel" data-bs-slide="next">
                <span class="carousel-control-next-icon"></span>
            </button>
        </div>
    </div>
</div>

<!-- Các danh mục sản phẩm -->
@foreach (var cat in Model)
{
    <div class="p-3 mb-4 rounded" style="background-color: #f5f5f5;"> <!-- Màu xám nhạt -->
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h5 class="fw-bold mb-0">@cat.CategoryName</h5>
            <a href="@Url.Action("Index", "Home", new { cat = cat.CategoryID })" class="btn btn-sm btn-outline-secondary">
                Xem thêm →
            </a>
        </div>

        <div class="row">
            @foreach (var p in cat.Products.Take(6))  
            {
                <div class="col-md-2 mb-3"> 
                    <div class="card h-100 shadow-sm border-0">
                        <img src="@(string.IsNullOrWhiteSpace(p.ImageURL) ? "/images/no-image.png" : p.ImageURL)"
                             class="card-img-top" style="height:120px; object-fit:cover;" alt="@p.ProductName" />
                        <div class="card-body p-2 d-flex flex-column">
                            <h6 class="card-title text-truncate mb-1" title="@p.ProductName">@p.ProductName</h6>
                            <p class="text-danger fw-bold mb-1">@p.Price.ToString("N0") ₫</p>
                            <a class="btn btn-outline-primary btn-sm mt-auto w-100"
                               href="@Url.Action("Index", "Product", new { id = p.ProductID })">
                                Chi tiết
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
}
